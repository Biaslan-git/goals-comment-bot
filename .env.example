# Groq API Configuration
GROQ_API_KEY=your-groq-api-key-here

# Optional: Proxy URL
PROXY_URL=

# Bot Tokens (comma-separated for multiple bots)
# Example with 1 bot:
# BOT_TOKENS=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
#
# Example with 3 bots:
# BOT_TOKENS=123456789:ABCdefGHIjklMNOpqrsTUVwxyz,987654321:ZYXwvuTSRqponMLKjihGFEdcBA,111222333:AAAbbCCddEEffGGhhIIjjKKllMM
#
# Example with 12 bots (your use case):
BOT_TOKENS=TOKEN1,TOKEN2,TOKEN3,TOKEN4,TOKEN5,TOKEN6,TOKEN7,TOKEN8,TOKEN9,TOKEN10,TOKEN11,TOKEN12

# Optional: Bot Names (comma-separated, must match number of tokens)
# If not provided, will auto-generate as Bot1, Bot2, Bot3, etc.
# BOT_NAMES=Motivator,Psychologist,Business,Fitness,Coach,Mentor,Advisor,Helper,Guide,Expert,Analyst,Consultant
BOT_NAMES=

# Optional: Admin User IDs (comma-separated, applies to ALL bots)
# Leave empty to allow everyone to use /setrole command
ADMIN_USER_IDS=

# Optional: Enable chat history for specific bots (comma-separated true/false)
# Must match the order and number of BOT_TOKENS
# Default: false for all bots if not specified
# Example for 3 bots where only first and third have history enabled:
# BOT_ENABLE_HISTORY=true,false,true
BOT_ENABLE_HISTORY=

# Optional: Use reply instead of answer for specific bots (comma-separated true/false)
# Must match the order and number of BOT_TOKENS
# Default: false for all bots if not specified (will use answer)
# Example for 3 bots where only second bot uses reply:
# BOT_USE_REPLY=false,true,false
BOT_USE_REPLY=

# Optional: Delete previous bot messages before sending new ones (comma-separated true/false)
# Must match the order and number of BOT_TOKENS
# Default: true for all bots if not specified (will delete previous messages)
# Example for 3 bots where only first bot deletes previous messages:
# BOT_DELETE_PREVIOUS=true,false,false
BOT_DELETE_PREVIOUS=

# Optional: Channel IDs to filter messages (comma-separated channel IDs or "none")
# Must match the order and number of BOT_TOKENS
# Default: none for all bots if not specified (will respond to all messages)
# If set, bot will ONLY respond to messages from the specified channel
# Useful for discussion groups attached to channels - bot will only reply to channel posts
# Example for 3 bots where first responds only to channel -1001234567890, others to all:
# BOT_CHANNEL_IDS=-1001234567890,none,none
# To get channel ID: forward a message from channel to @userinfobot
BOT_CHANNEL_IDS=

# Optional: Use markdown formatting in bot responses (comma-separated true/false)
# Must match the order and number of BOT_TOKENS
# Default: false for all bots if not specified (plain text responses)
# Example for 3 bots where only first and second use markdown:
# BOT_USE_MARKDOWN=true,true,false
BOT_USE_MARKDOWN=

# Chat history limit (max messages to keep in context)
# Default: 20
CHAT_HISTORY_LIMIT=20

# Data directory (don't change unless you know what you're doing)
DATA_DIR=/data
